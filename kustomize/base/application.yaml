apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: simple-app-vela
  namespace: default
spec:
  components:
    - name: simple-app-helm
      type: helm
      properties:
        repoType: "git"
        url: "git@github.com:sirvaulterscoff/argocd-poc.git"   # заменить на ваш репо
        chart: "./charts/simple-app"
        git:
          branch: main
        values:
          image:
            repository: git@github.com:sirvaulterscoff/argocd-poc.git  # туда будет пушиться образ из CI
            tag: "v1"                                      # будет обновляться из kustomize overlay
